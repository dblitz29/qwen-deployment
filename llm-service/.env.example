# LLM Service Configuration

# Path to GGUF model file (inside container)
# For split files, the entrypoint will automatically merge them
MODEL_PATH=/models/qwen2.5-7b-instruct-q4_k_m.gguf

# Example model files (download from HuggingFace):
# - Qwen2.5-7B-Instruct-GGUF (requires merge for split files)
# - Download: huggingface-cli download Qwen/Qwen2.5-7B-Instruct-GGUF --local-dir models
